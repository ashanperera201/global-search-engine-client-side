# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: app-client-pvc # Azure-managed-disk
#   namespace: deep-crawler
#   labels:
#     app: app-client
# spec:
#   accessModes:
#   - ReadWriteOnce
#   storageClassName: managed-premium # "" # Empty string must be explicitly set otherwise default StorageClass will be set
#   resources:
#     requests:
#       storage: 2Gi
#   # To make sure we match the claim with the exact volume, match the label
#   selector:
#     matchLabels:
#       usage: app-client-pvc